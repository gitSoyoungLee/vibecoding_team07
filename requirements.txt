# core
pandas>=1.5

# optional: DB에 적재할 때 필요 (주석 해제해서 사용)
# SQLAlchemy>=1.4
# PyMySQL>=1.0

# 추천(환경에 따라)
# python-dotenv>=1.0  # DB 접속정보를 .env에서 불러올 경우

fastapi>=0.95
uvicorn[standard]>=0.18
SQLAlchemy>=1.4
python-dotenv>=1.0